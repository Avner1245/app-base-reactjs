name: build and test

on:
  push:

jobs:
    build-and-test:
      runs-on: ubuntu-latest

      steps:
        - name: checkout
          uses: Actions/checkout@v4

        - name: Setup Node
          uses: Actions/Setup-node@v4
          with:
            node-version: 20
            cache: npm  


